- name: Mostrar interfaces en CISCO
  hosts: MPLS_update
  gather_facts: false

  tasks:
    - name: mostrar interfaces en todos los MPLS_update
      ios_command:
        commands: "show ip interface brief"
      register: resultado
    
    - name: Imprimir en la terminal
      debug:
        var: resultado.stdout_lines
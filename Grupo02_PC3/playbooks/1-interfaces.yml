---
- name: Playbook 1 - Configuracion de Interfaces
  hosts: MPLS_update
  gather_facts: false
  tasks:

    # ──────────────────────────────────────────────
    # PE1
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en PE1
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Loopback0
          lines:
            - ip address 172.16.1.1 255.255.255.255
            - no shutdown

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con RR1
            - ip address 10.0.0.1 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con P1
            - ip address 10.0.0.5 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/3
          lines:
            - description Conexion con P2
            - ip address 10.0.0.9 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown
      when: inventory_hostname == "clab-MPLS-PE1"

    # ──────────────────────────────────────────────
    # PE2
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en PE2
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Loopback0
          lines:
            - ip address 172.16.1.2 255.255.255.255
            - no shutdown

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con P1
            - ip address 10.0.0.30 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con P2
            - ip address 10.0.0.34 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/3
          lines:
            - description Conexion con RR2
            - ip address 10.0.0.38 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown
      when: inventory_hostname == "clab-MPLS-PE2"

    # ──────────────────────────────────────────────
    # P1
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en P1
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Loopback0
          lines:
            - ip address 172.16.1.4 255.255.255.255
            - no shutdown

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE1
            - ip address 10.0.0.6 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con RR1
            - ip address 10.0.0.18 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/3
          lines:
            - description Conexion con P2
            - ip address 10.0.0.21 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet1/0
          lines:
            - description Conexion con RR2
            - ip address 10.0.0.25 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet1/1
          lines:
            - description Conexion con PE2
            - ip address 10.0.0.29 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown
      when: inventory_hostname == "clab-MPLS-P1"

    # ──────────────────────────────────────────────
    # P2
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en P2
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Loopback0
          lines:
            - ip address 172.16.1.3 255.255.255.255
            - no shutdown

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE1
            - ip address 10.0.0.10 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con P1
            - ip address 10.0.0.22 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/3
          lines:
            - description Conexion con PE2
            - ip address 10.0.0.33 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown
      when: inventory_hostname == "clab-MPLS-P2"

    # ──────────────────────────────────────────────
    # RR1
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en RR1
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Loopback0
          lines:
            - ip address 172.16.1.5 255.255.255.255
            - no shutdown

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE1
            - ip address 10.0.0.2 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con P1
            - ip address 10.0.0.17 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/3
          lines:
            - description Conexion con RR2
            - ip address 10.0.0.13 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown
      when: inventory_hostname == "clab-MPLS-RR1"

    # ──────────────────────────────────────────────
    # RR2
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en RR2
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Loopback0
          lines:
            - ip address 172.16.1.6 255.255.255.255
            - no shutdown

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con RR1
            - ip address 10.0.0.14 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con P1
            - ip address 10.0.0.26 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown

        - parent: interface Ethernet0/3
          lines:
            - description Conexion con PE2
            - ip address 10.0.0.37 255.255.255.252
            - ip ospf network point-to-point
            - no shutdown
      when: inventory_hostname == "clab-MPLS-RR2"

    # ──────────────────────────────────────────────
    # CPE-1
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en CPE-1
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE1
            - ip address 10.0.1.2 255.255.255.252
            - ipv6 address FC10::2/64
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con LAN
            - ip address 192.168.10.1 255.255.255.0
            - ipv6 address 2001:192:168:10::1/64
            - no shutdown
      when: inventory_hostname == "clab-MPLS-CPE-1"

    # ──────────────────────────────────────────────
    # CPE-2
    # ──────────────────────────────────────────────
    - name: Configurar interfaces en CPE-2
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE2
            - ip address 10.0.2.2 255.255.255.252
            - ipv6 address FC20::2/64
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con LAN
            - ip address 192.168.20.1 255.255.255.0
            - ipv6 address 2001:192:168:20::1/64
            - no shutdown
      when: inventory_hostname == "clab-MPLS-CPE-2"

    # ──────────────────────────────────────────────
    # SW-1
    # ──────────────────────────────────────────────
    - name: Configurar VLAN en SW-1
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: vlan 10
          lines:
            - name Cliente_A

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE1
            - switchport trunk encapsulation dot1q
            - switchport mode trunk
            - switchport trunk allowed vlan add 10
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con Cliente A
            - switchport mode access
            - switchport access vlan 10
            - no shutdown
      when: inventory_hostname == "clab-MPLS-SW-1"

    # ──────────────────────────────────────────────
    # SW-2
    # ──────────────────────────────────────────────
    - name: Configurar VLAN en SW-2
      cisco.ios.ios_config:
        parents: "{{ item.parent }}"
        lines: "{{ item.lines }}"
      loop:
        - parent: vlan 10
          lines:
            - name Cliente_A

        - parent: interface Ethernet0/1
          lines:
            - description Conexion con PE2
            - switchport trunk encapsulation dot1q
            - switchport mode trunk
            - switchport trunk allowed vlan add 10
            - no shutdown

        - parent: interface Ethernet0/2
          lines:
            - description Conexion con Cliente A
            - switchport mode access
            - switchport access vlan 10
            - no shutdown
      when: inventory_hostname == "clab-MPLS-SW-2"